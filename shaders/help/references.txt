/* DRAWBUFFERS:NNNN */      saves the index of a FragData[N] and assigns it to the following index

dot(x, y)                   takes all channels of x, and multiplies each channel by y 

DRAWBUFFERS:
0 - final
1 - normal
2 - lightmap
3 - bloom texture
4 - material